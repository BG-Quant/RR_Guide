const $=s=>document.querySelector(s);let activeRole=0,filter='all';const esc=s=>s.replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function render(){const r=GUIDELINES.roles[activeRole];$('#roles').innerHTML=GUIDELINES.roles.map((x,i)=>`<button data-role="${i}" class="${i===activeRole?'active':''}" aria-pressed="${i===activeRole}"><span>${'ABC'[i]}</span>${esc(x.name)}</button>`).join('');$('#title').textContent='For '+r.name.toLowerCase();$('#breadcrumb').textContent=r.name;$('#intro').textContent=r.intro;$('#part').textContent='ABC'[activeRole];const q=$('#search').value.trim().toLowerCase();const sections=r.sections.map((s,i)=>({...s,index:i})).filter(s=>(filter==='all'||s[filter].length)&&[s.title,...s.required,...s.recommended,...s.notes].join(' ').toLowerCase().includes(q));$('#count').textContent=`${sections.length} topics`;$('#content').innerHTML=sections.map((s,i)=>`<details ${i===0||q?'open':''}><summary><span class="num">${String(s.index+1).padStart(2,'0')}</span>${esc(s.title)}<span class="arrow" aria-hidden="true">＋</span></summary><div class="detail-body">${['required','recommended','notes'].filter(k=>s[k].length&&(filter==='all'||filter===k)).map(k=>`<section class="guidance ${k}"><h3>${k==='required'?(activeRole===2?'Core responsibilities':'Minimum requirement'):k==='recommended'?(activeRole===2?'Enhanced implementation practices':'Recommended practice'):'Additional guidance'}</h3>${s[k].map(t=>`<p>${esc(t)}</p>`).join('')}</section>`).join('')}</div></details>`).join('')||'<p class="empty">No matching guidance. Try another search or guidance level.</p>';$('#expand').textContent='Expand all';}
$('#roles').onclick=e=>{const b=e.target.closest('[data-role]');if(!b)return;activeRole=Number(b.dataset.role);$('#search').value='';render();};$('#search').oninput=render;$('#filters').onclick=e=>{const b=e.target.closest('[data-filter]');if(!b)return;filter=b.dataset.filter;document.querySelectorAll('[data-filter]').forEach(x=>{x.classList.toggle('active',x===b);x.setAttribute('aria-pressed',x===b)});render()};$('#expand').onclick=()=>{const expanded=$('#expand').textContent==='Expand all';document.querySelectorAll('details').forEach(d=>d.open=expanded);$('#expand').textContent=expanded?'Collapse all':'Expand all'};$('#references').onclick=()=>$('#refDialog').showModal();$('#closeRefs').onclick=()=>$('#refDialog').close();$('#refList').innerHTML=GUIDELINES.refs.map(r=>`<li>${esc(r)}</li>`).join('');render();
